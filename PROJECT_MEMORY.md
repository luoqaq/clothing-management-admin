# 服装管理后台前端 - 项目记忆

最近更新：2026-04-05

## 会话更新（2026-04-05）
- 已修复后台商品标签在 iPad Safari 下载 PNG 时二维码缺失的问题：
  - `src/components/ProductLabelPrintModal.tsx` 已移除对 `html-to-image` 的标签导出依赖
  - 标签下载改为使用原生 canvas 按固定版式绘制并导出 PNG，不再依赖 DOM 截图
  - 下载按钮已增加“二维码全部生成完成后才可点击”的保护，避免导出时机过早导致空二维码
  - 已进一步把标签预览 DOM 与 canvas 导出收敛到同一套共享布局常量，减少横线、边距、二维码区等样式漂移
  - 后续已调整为“DOM 排版 + 二维码 canvas + DOM 导出”方案，避免标题字体在整图 canvas 导出中回退失真
- 本次验证已执行：
  - `npm run build` 通过
- 遗留注意：
  - 当前仍沿用浏览器侧逐张触发下载的方式；若后续 iPad 上出现多文件下载拦截，可进一步改成单文件 ZIP 打包下载或后端生成文件

## 会话更新（2026-04-05）
- 已将后台扫码录单改动推送到远端主分支：
  - 当前提交：`19d2337`
- 已完成生产前端发布并验证：
  - 线上前端仓库 `/var/clothing/admin` 已更新到 `19d2337`
  - `https://clothing.chuchu9.cn` 返回 `200`
- 本次验证已执行：
  - 本地 `npm run build` 通过
  - 线上 `curl -I https://clothing.chuchu9.cn` 返回 `200`

## 会话更新（2026-04-05）
- 已为后台订单新建页补充 web 端扫码录单能力：
  - `src/pages/Orders/OrderForm.tsx` 新增“扫码录单”输入区，支持 PAD 上使用扫码枪输入标签码后直接回车带入规格
  - 扫码命中已在订单内的同一规格时，不再拦截，改为自动累加数量并受可售库存上限约束
  - 页面会展示最近一次扫码结果，方便门店连续录单时快速确认
- 已为后台前端补充扫码商品类型与接口封装：
  - `src/types/index.ts` 新增 `ScannedSkuProduct`
  - `src/api/products.ts` 新增通过现有后端移动端接口 `/api/mobile/products/by-code` 查询标签码的能力
- 本次验证已执行：
  - `npm run build` 通过
- 遗留注意：
  - 当前后台 web 端复用的是现有移动端扫码查询接口，后续若要彻底统一语义，可考虑把 `/products/by-code` 提升为后台/移动共用商品路由能力

## 会话更新（2026-04-04）
- 已完成后台商品标签打印第一期：
  - 商品管理列表操作区新增“打印标签”
  - 商品详情弹窗内新增“打印当前商品标签”
  - 新增组件：`src/components/ProductLabelPrintModal.tsx`
- 标签预览当前能力：
  - 按商品读取全部 SKU 标签数据
  - 每张标签现仅展示二维码、人眼编码、商品名、款号、颜色、尺码、销售价与主图
  - 已隐藏一维条码
  - 不展示成本价
  - 已从“浏览器打印”调整为“导出 PNG 标签图下载”
  - 标签图按 `40×60` 纵向比例输出
- 商品表单已移除“手填条码”的主流程：
  - 规格区域改为展示“标签码：保存后自动生成 / 已生成编码”
  - 编辑商品时会继续显示已存在的标签码状态
- 已新增前端依赖：
  - `qrcode`
  - `jsbarcode`
  - `html-to-image`
- 本次验证已执行：
  - `npm run build` 通过

## 会话更新（2026-04-04）
- 已为后台顶部栏新增手动折叠开关：
  - 头部现支持在展开 / 折叠两种状态间切换
  - 折叠后不再保留整条顶部栏，仅保留一枚悬浮展开按钮
  - 内容区顶部间距继续跟随顶部实际占位高度变化，避免 pad 等小屏设备上顶部栏长期遮挡列表
- 本次验证已执行：
  - `npm run build` 通过

## 会话更新（2026-04-01）
- 已按经营分析口径重构统计页：
  - 统计页改为“销售统计 / 成本统计”两个一级 Tab
  - 销售统计新增：总销售额、订单数、客单价、客户数、新老客户、年龄分层、商品类别销售情况、商品销售排行榜、总毛利、商品毛利分析
  - 成本统计新增：总成本、商品分类成本、商品成本排行榜
- 已同步扩展系统配置页：
  - 新增“客户年龄段”配置入口
  - 新增“客户档案”管理入口，可维护客户姓名、邮箱、年龄段
- 统计页前端状态已从旧的单块销售数据结构扩展为模块化数据结构，供销售/成本两个 Tab 分开渲染
- 本次验证已执行：
  - `npm run build` 通过

## 会话更新（2026-04-01）
- 已调整数据统计页为双 Tab 展示：
  - 新增“销售统计”Tab，保留销售趋势、分类/区域销售分析、商品销售排行
  - 新增“成本统计”Tab，展示总成本、成本趋势、分类/区域成本分析、商品成本排行
- 已补充统计页概览卡片：
  - 销售 Tab 展示总销售额、支付订单数、支付客户数、平均客单价
  - 成本 Tab 展示总成本、支付订单数、支付客户数、平均单笔成本
- 本次验证已执行：
  - `npm run build` 通过

## 会话更新（2026-04-01）
- 已调整商品列表页的规格展示方式：
  - “规格”列改为直接逐行罗列当前商品的全部规格
  - 不再仅展示前两项，也不再显示“等 N 个规格”
- 本次验证已执行：
  - `npm run build` 通过

## 会话更新（2026-04-01）
- 已同步前端“款号可重复”规则：
  - 商品批量导入页不再把“同批款号重复”或“规格编码重复”作为前端拦截错误
  - 仍保留同一商品内颜色/尺码组合去重、必填项、分类/供应商匹配等校验
- 已调整商品表单中的规格编码提示文案：
  - 规格编码输入框改为提示“保存后自动生成最终规格编码”，避免继续暗示仅按“款号-尺码-颜色”生成
- 本次验证已执行：
  - `npm run build` 通过

## 会话更新（2026-04-01）
- 已移除登录页中的默认账号明文提示：
  - 后台登录页不再展示默认管理员账号信息
  - 避免在界面上继续暴露默认管理员凭据
- 本次验证已执行：
  - `npm run build` 通过

## 会话更新（2026-04-01）
- 已完成管理员/销售角色 v1 前端适配：
  - 认证角色已补充 `sales`，并将历史 `manager/staff` 统一按销售展示
  - 侧边导航已改为按角色生成：销售仅保留“数据概览 / 商品管理 / 订单管理”
  - `ProtectedRoute` 已收口销售对 `/statistics`、`/configuration`、商品导入/新建/编辑等管理员页面的直接访问
- 已完成商品信息前端脱敏：
  - 销售端不再显示供应商筛选、供应商信息、规格成本价
  - 销售端不再显示商品批量导入、新建、编辑、删除、调整库存入口
- 已在系统配置页新增“销售账号”管理：
  - 管理员可查看销售账号列表
  - 支持新增销售账号
  - 支持编辑用户名、姓名并可选重置密码
- 本次验证已执行：
  - `npm run build` 通过

## 会话更新（2026-04-01）
- 已收敛前端锁文件漂移风险：
  - `package.json` 已显式声明 `packageManager=npm@10.8.2`
  - 后端仓库中的统一发布脚本已改为前端使用 `npm ci`，不再在生产机通过 `npm install` 回写 `package-lock.json`
- 本次验证已执行：
  - `npm run build` 通过
- 注意：
  - 当前生产机 `/var/clothing/admin` 仍遗留历史 `package-lock.json` 漂移与 stash 记录；在同步本次脚本改动前，旧发布流程仍可能再次遇到脏工作区问题

## 会话更新（2026-03-31）
- 已修复商品管理页的筛选与分页联动问题：
  - 商品列表页改为基于当前界面筛选值构造请求参数，翻页时会继续携带搜索词、分类、供应商、状态
  - 分类 / 供应商 / 状态筛选切换后会立即触发请求，不再必须再点一次“筛选”
  - 商品 Redux slice 在列表请求成功后会同步记录本次 filters，避免页码切换时丢失条件
- 已调整新建商品规格默认值：
  - 新建商品首条规格的售价、成本价、库存默认为空
  - 新增规格时会自动继承上一条规格的售价、成本价、库存
- 已补上商品相关操作失败原因提示：
  - 新增 / 编辑 / 删除商品、调整库存、导入、分类与供应商维护失败时，会直接提示后端返回的具体原因
  - 已新增 `src/utils/error.ts` 统一提取接口报错信息，避免被笼统“网络错误”覆盖
- 本次验证已执行：
  - `npm run build` 通过

## 会话更新（2026-03-31）
- 已将前端批量导入能力推送到远端主分支：
  - 当前提交：`6661d40`
- 已完成生产前端发布并验证：
  - 线上前端仓库 `/var/clothing/admin` 已更新到 `6661d40`
  - `curl -I -H 'Host: clothing.chuchu9.cn' http://127.0.0.1/` 返回 `200`
  - `curl -I http://clothing.chuchu9.cn` 返回 `200`
- 本次线上部署遇到一处流程性问题：
  - `deploy/release.sh` 执行 `git pull --ff-only` 前，前端工作区已有 `package-lock.json` 本地漂移，导致首次发布中断
  - 本次通过对锁文件改动执行临时 `git stash` 后继续发布成功
  - 后续应优先把发布脚本补成“先检测脏工作区并明确报错或自动 stash”，避免再次被锁文件拦截

## 会话更新（2026-03-31）
- 已新增商品批量导入页面与入口：
  - 商品列表页已新增“批量导入”按钮，路由为 `/products/import`
  - 新页面支持三步流程：上传资料、审核编辑、导入结果
- 已支持两种导入来源：
  - Excel：前端本地解析 `.xlsx/.xls` 后调用后端解析接口
  - 截图：前端上传图片给后端，由后端调用 AI 识别
- 已新增批量导入审核能力：
  - 可编辑商品基础信息、规格明细
  - 支持删除商品草稿、增删规格、批量设置默认分类/状态
  - 支持“自动创建缺失供应商”开关
  - 前端会对分类缺失、款号重复、规格重复、必填字段缺失做提交前校验
- 已在商品模块新增批量导入 API 封装与 Redux thunk：
  - `parseExcelImport`
  - `parseImageImport`
  - `bulkCreateProducts`
- 已新增前端依赖：
  - `xlsx`
- 本次验证已执行：
  - `npm run build` 通过

## 会话更新（2026-03-31）
- 已调整商品表单与订单表单的主操作区位置：
  - 商品新增/编辑页的“保存商品”按钮已从底部移到表单顶部右上角
  - 订单新建页的“创建订单”按钮已从底部移到表单顶部右上角
  - 顶部操作区已改为吸顶固定，滚动长表单时按钮会持续停留在顶部
  - 移动端下操作区自动左对齐，避免窄屏布局拥挤
  - 已补充 pad / 手机端适配：
    - 商品、订单相关弹窗宽度会按断点自适应收缩
    - 订单表单的支付信息、客户信息在窄屏下改为单列展示
    - 手机端顶部操作按钮改为等宽铺满，点击区域更稳定
  - 已扩展为整站响应式适配：
    - `lg` 以下切换为抽屉式侧边导航，支持遮罩关闭与导航后自动收起
    - 头部、内容区、筛选栏、Tabs、表格滚动区、卡片间距、弹窗边距已统一适配 pad / 手机端
    - 统计页分类分析区与排行表、基础资料页管理表已补充窄屏布局和横向滚动兜底
  - 商品规格表单中的“售价 / 成本价”输入框已调整显示格式：
    - 默认值显示为 `0`，不再强制显示为 `0.00`
    - 仍保留最多两位小数输入能力
  - 新建商品表单的默认商品状态已从“草稿”调整为“上架中”
- 本次验证已执行：
  - `npm run build` 通过

## 会话更新（2026-03-31）
- 已完成生产前端目录部署方式切换：
  - `/var/clothing/admin` 已切换为标准 git 工作区
  - 远端改为 `https://github.com/luoqaq/clothing-management-admin.git`
  - 当前线上前端仓库 HEAD：`4c877ad`
- 已在生产机重新完成前端构建：
  - 通过 `PATH=/usr/local/node20-bin:$PATH` 显式使用 Node 20 / npm 20 执行构建
  - 重新产出 `/var/clothing/admin/dist`
- 本次线上验证已执行：
  - `curl -I http://clothing.chuchu9.cn` 返回 `200`
  - `POST http://clothing.chuchu9.cn/api/auth/login` 返回 `200`
- 注意：
  - 当前生产机默认 `node` 仍指向 Node 18，前端构建必须显式切到 `/usr/local/node20-bin`
  - 若后续前端首页再次出现 `500`，优先检查 `/var/clothing/admin/dist` 是否存在，再检查 Node 版本和构建命令

## 会话更新（2026-03-30）
- 已完成前端构建体积优化：
  - 移除了 `echarts-for-react`，改为按需引入 ECharts 核心模块
  - 创建了 `src/components/ECharts/index.tsx` 封装组件，仅注册项目需要的图表类型（LineChart, BarChart, PieChart）
  - 优化了 `vite.config.ts`，添加 `manualChunks` 配置实现精细化的代码分割：
    - `vendor-react`：React 核心库
    - `vendor-redux`：Redux 相关
    - `vendor-antd`：Ant Design 及其图标
    - `vendor-echarts`：ECharts 图表库（约 558KB）
    - `vendor-utils`：工具库（dayjs, axios, zod 等）
    - `module-statistics/products/orders`：业务模块独立打包
  - 更新了 `Dashboard.tsx` 和 `Statistics/index.tsx`，使用新的 ECharts 组件
- 优化效果：
  - 原 `useStatistics` chunk 1,131KB → 现 `module-statistics` 仅 12KB
  - ECharts 独立打包 558KB，业务页面体积大幅减少
  - 统计页面仅 5KB，工作台页面仅 5KB
- 构建警告仍存在（Ant Design 全量引入约 1.2MB），但已远低于优化前水平
- 本次验证已执行：
  - `npm run build` 通过

## 会话更新（2026-03-27）
- 商品主数据已从“品牌”切换为“供应商”。
- 前端已同步改造以下链路：
  - 商品类型、筛选参数、详情展示、表单提交字段改为 `supplierId` / `supplier`
  - 商品 API 从 `/products/brands` 切换为 `/products/suppliers`
  - 基础资料页已将“品牌管理”替换为“供应商管理”，表单仅维护 `name`
  - 商品列表与商品表单已改用供应商选择与展示
- mock 商品接口与 mock 商品主数据也已同步切换为供应商；统计 mock 仍保留原 `brand-sales` 接口名，但底层已改为按供应商数据生成，避免编译受阻。
- 本次验证已执行：
  - `npm run build` 通过
- ~~构建阶段仍有大体积 chunk warning，当前未处理，不阻塞本次提交。~~（已在 2026-03-30 优化）

## 会话更新（2026-03-23）
- 确认当前未提交改动主要包括三类：
  - 订单表单 `Select` 样式微调与页面文案/间距调整
  - `useOrders` 中移除 `shipOrder` 导出
  - 订单归一化增加 `address` 空值兜底
- 执行 `npm run build` 通过，可正常产出前端构建结果。
- 构建阶段仍有大体积 chunk warning，当前未处理，不阻塞本次提交。

## 会话更新（2026-03-20）
- 已将 `AGENTS.md` 与 `PROJECT_MEMORY.md` 统一为中文版本，保留命令与路径原文。
- 已新增仓库级 `AGENTS.md`，并要求固定流程：
  - 会话开始先读 `PROJECT_MEMORY.md`
  - 会话结束前更新 `PROJECT_MEMORY.md`
- 已在 `AGENTS.md` 增加前端专项规则：
  - 默认连接真实后端，除非用户明确要求否则不启用 mock
  - 固定运行/构建命令与验证要求
  - 变更边界、安全规则、Git 规则
  - 收尾回复格式与记忆更新模板

## 仓库信息
- 路径：`/Users/luo/Project/clothing-management-admin`
- 远程：`git@github.com:luoqaq/clothing-management-admin.git`
- 默认分支：`main`

## 启动手册
- 安装依赖：`npm install`
- 开发启动：`npm run dev -- --host 127.0.0.1 --port 5173`
- 构建：`npm run build`
- 本地地址：`http://127.0.0.1:5173/`

## API 集成
- `VITE_API_BASE_URL=/api`（位于 `.env.development`）
- Vite 代理：`/api -> http://localhost:3000`（见 `vite.config.ts`）
- 当前模式：直连后端（`src/main.tsx` 已移除 mock 启动逻辑）

## 本地测试账号
- 默认登录：`chuchu / chuchu0510`

## 关键决策
- 前端 `.env` 文件允许提交（当前团队约定）。
- 已加入金额字段归一化，避免后端返回字符串时触发 `toFixed` 崩溃：
  - `src/utils/normalize.ts`
  - `src/api/products.ts`
  - `src/api/orders.ts`
  - `src/pages/Products/List.tsx` 也加入了页面级兜底格式化

## 已知问题与风险
- 若金额格式相关报错再次出现，先检查 API 字段类型（`string` 或 `number`），再扩展归一化处理。
- 若前端页面可访问但接口失败，优先确认后端是否监听 `3000` 端口。

## 快速验证
1. 先启动后端。
2. 再启动前端。
3. 打开商品列表，确认不再出现 `price.toFixed` 报错。
4. 使用默认管理员账号登录，确认数据来自真实后端。

## 最近变更补充
- 数据统计页已重构为 `销售统计 / 成本统计` 两个一级 Tab。
- 成本统计口径已调整为“商品库存成本视角”，不依赖订单：
  - 总成本 = 规格 `cumulativeCostAmount` 汇总
  - 商品分类成本 = 分类下规格累计成本汇总
  - 商品成本排行榜 = 按规格展示 `当前库存 / 累计入库 / 成本单价 / 累计成本`
- 成本统计已改为“累计入库成本”口径：
  - 销售出库不会冲减累计成本
  - 手工补库存时会继续累计成本
- 新增订单表单的客户信息区已增加“年龄段”可选下拉，数据来源于客户年龄段配置。
- 创建订单时若填写了手机号且订单为已支付，会在客户归并时把所选年龄段同步写入客户档案。
- 客户档案入口已从“系统配置”迁移到“数据统计”页，作为只读展示型 Tab：
  - `数据统计 > 客户档案`
  - 系统配置页不再承载客户档案管理入口
- 本轮已执行 `npm run build` 验证通过。
